/*const garbage-can = document.getElementById('garbage-can');
const pizza-mobile = document.getElementById('pizza-mobile');


function checkCollision() {
  const garbage-canRect = getBoundingClientRect()'
  const pizza-mobile = getBoundingClientRect();

  if (
    pizza-mobileRect.left <= garbage-can 
)

}
